export enum Role {
  Admin = 'admin',
  Manager = 'manager',
  Operator = 'operator',
  Viewer = 'viewer',
  // Proptech-specific
  AgencyAdmin = 'agency_admin',
  Agent = 'agent',
  Owner = 'owner',
  Client = 'client',
  LegalReviewer = 'legal_reviewer',
}
