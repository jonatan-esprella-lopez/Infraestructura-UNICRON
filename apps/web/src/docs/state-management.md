# State Management

La base incluye `RootStoreProvider` para identidad y permisos. Si el reto requiere estado complejo, cada modulo ya tiene carpeta `store` para migrar a Zustand, Redux Toolkit o TanStack Query sin mezclar dominios.
