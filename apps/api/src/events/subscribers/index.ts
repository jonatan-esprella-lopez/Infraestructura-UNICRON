import type { AppServices } from '../../core/types/api.types.js';

export function registerSubscribers(_services: AppServices): void {
  // Cross-module subscribers are registered by module factories.
}
