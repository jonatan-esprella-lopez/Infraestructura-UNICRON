import type { AppServices } from '../../core/types/api.types.js';

export function registerJobWorkers(_services: AppServices): void {
  // Wire background workers here when persistent queues are enabled.
}
