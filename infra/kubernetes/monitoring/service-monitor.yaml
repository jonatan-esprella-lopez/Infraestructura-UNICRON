apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: api
  namespace: unicron
spec:
  selector:
    matchLabels:
      app: api
  endpoints:
    - port: http
      path: /metrics
